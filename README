This is a project to run a server via flask that reads a single Seeed water sensor on pin 11 and says weather or not water is on the sensor

Raspberry pi password: pennypoodle

To run the server to any device on the local network, run:

	flask run --host=0.0.0.0



